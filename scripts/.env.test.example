# Copier vers .env.test (non versionné) puis lancer (depuis scripts/) :
#   Get-Content .env.test | ForEach-Object { if ($_ -match '^\s*([^#][^=]+)=(.*)$') { Set-Item -Path "env:$($matches[1].Trim())" -Value $matches[2].Trim() } }
#   .\test-api-integration.ps1

# Local (apres migrate --seed) :
API_BASE_URL=http://localhost:8000
API_PREFIX=/api/v1

TEST_ADMIN_EMAIL=admin@test.com
TEST_ADMIN_PASSWORD=password

TEST_SUPPLIER_EMAIL=fournisseur@test.com
TEST_SUPPLIER_PASSWORD=password

TEST_BUTCHER_EMAIL=boucher@test.com
TEST_BUTCHER_PASSWORD=password

# Render (prod) :
# API_BASE_URL=https://boucherie-api.onrender.com
# TEST_ADMIN_EMAIL=herve@example.net
# TEST_ADMIN_PASSWORD=admin1234
